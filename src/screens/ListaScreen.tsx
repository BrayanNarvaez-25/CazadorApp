import { Text, View } from 'react-native';

export default function ListaScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Lista (pendiente)</Text>
    </View>
  );
}